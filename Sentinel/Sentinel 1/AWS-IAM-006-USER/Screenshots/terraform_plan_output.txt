terraform plan -out=awsiamuser006

Terraform used the selected providers to generate the following execution plan. Resource actions are indicated with the
following symbols:
  + create
 <= read (data resources)

Terraform will perform the following actions:

  # data.aws_iam_policy_document.casper_policy will be read during apply
  # (config refers to values not yet known)
 <= data "aws_iam_policy_document" "casper_policy" {
      + id      = (known after apply)
      + json    = (known after apply)
      + version = "2012-10-17"

      + statement {
          + actions   = [
              + "*",
            ]
          + effect    = "Allow"
          + resources = [
              + "*",
            ]
          + sid       = "AWS IAM overprivileged access"

          + principals {
              + identifiers = [
                  + (known after apply),
                ]
              + type        = "AWS"
            }
        }
    }

  # aws_iam_user.casper_iam will be created
  + resource "aws_iam_user" "casper_iam" {
      + arn           = (known after apply)
      + force_destroy = false
      + id            = (known after apply)
      + name          = "casper_iam"
      + path          = "/system/"
      + tags          = {
          + "environment" = "test"
        }
      + tags_all      = {
          + "environment" = "test"
        }
      + unique_id     = (known after apply)
    }

  # aws_iam_user_policy.casper_testing_policy will be created
  + resource "aws_iam_user_policy" "casper_testing_policy" {
      + id     = (known after apply)
      + name   = "casper_check_policy"
      + policy = (known after apply)
      + user   = "casper_iam"
    }

Plan: 2 to add, 0 to change, 0 to destroy.