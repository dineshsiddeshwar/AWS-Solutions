{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "API GW least privileged model Access",
            "Effect": "Allow",
            "Principal": {
                "AWS": "${api_gateway_role}"
            },
            "Resource": "${api_gateway_arn}",
            "Action": [
                "execute-api:Invoke"
            ]
        },
        {
            "Sid": "Deny EFS access from anywhere except VPC Endpoint",
            "Effect": "Deny",
            "Principal": {
                "AWS": "*"
            },
            "Resource": "${api_gateway_arn}",
            "Action": [
                "*"
            ],
            "Condition": {
                "StringNotEquals": {
                    "aws:sourceVpce": "${vpc_endpoint_id}"
                }
            }
        },
        {
            "Sid": "Deny Root File system access",
            "Effect": "Deny",
            "Principal": {
                "AWS": "*"
            },
            "Resource": "${api_gateway_arn}",
            "Action": [
                "elasticfilesystem:ClientRootAccess"
            ]
        },
        {
            "Sid": "Deny EFS access if connection is not TLS1.2",
            "Effect": "Deny",
            "Principal": {
                "AWS": "*"
            },
            "Resource": "${api_gateway_arn}",
            "Action": [
                "*"
            ],
            "Condition": {
                "Bool": {
                    "aws:SecureTransport": "false"
                }
            }
        }
    ]
}
